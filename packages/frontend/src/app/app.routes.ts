import { Routes } from '@angular/router';
import { MeetingPipelineComponent } from './meeting-pipeline/meeting-pipeline.component';

export const routes: Routes = [
    { path: '', component: MeetingPipelineComponent }
];
